// eslint-disable-next-line import/no-extraneous-dependencies -- goes with tamagui
import { styled } from '@tamagui/core';
import { Text as _Text } from 'tamagui';

export const Text = styled(_Text, {});
